---
// Hero section for biblical stories in Quechua for children
---

<section id="hero" class="relative min-h-screen flex items-center justify-center overflow-hidden bg-gradient-to-br from-orange-50 via-amber-50 to-yellow-50">
  <!-- Background decorative elements -->
  <div class="absolute inset-0 overflow-hidden">
    <!-- Floating clouds -->
    <div class="absolute top-20 left-10 w-24 h-16 bg-white/30 rounded-full blur-xl animate-pulse" style="animation-delay: 0s;"></div>
    <div class="absolute top-32 right-20 w-32 h-20 bg-white/40 rounded-full blur-xl animate-pulse" style="animation-delay: 1s;"></div>
    <div class="absolute bottom-32 left-1/4 w-20 h-12 bg-white/25 rounded-full blur-xl animate-pulse" style="animation-delay: 2s;"></div>
    
    <!-- Decorative stars -->
    <div class="absolute top-1/4 right-1/4 w-2 h-2 bg-yellow-300 rounded-full animate-twinkle"></div>
    <div class="absolute top-1/3 left-1/3 w-1.5 h-1.5 bg-yellow-200 rounded-full animate-twinkle" style="animation-delay: 0.5s;"></div>
    <div class="absolute bottom-1/4 right-1/3 w-1 h-1 bg-yellow-400 rounded-full animate-twinkle" style="animation-delay: 1s;"></div>
  </div>

  <!-- Main content -->
  <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
      <!-- Left side - Text content -->
      <div class="text-center lg:text-left">
        <!-- Welcome message in Quechua -->
        <div class="mb-8">
          <h2 class="text-lg md:text-xl font-medium text-orange-600 mb-2 animate-fade-in-up">
            ¡Allillanchu! (¡Hola!)
          </h2>
          <p class="text-sm md:text-base text-orange-500 animate-fade-in-up" style="animation-delay: 0.2s;">
            Bienvenidos a nuestras historias sagradas
          </p>
        </div>

        <!-- Main title -->
        <h1 class="text-4xl md:text-6xl lg:text-7xl font-bold text-teal-800 mb-6 leading-tight animate-fade-in-up" style="animation-delay: 0.4s;">
          <span class="block">Historias Bíblicas</span>
          <span class="block text-orange-500">en Quechua</span>
          <span class="block text-2xl md:text-3xl lg:text-4xl text-teal-600 mt-4 font-normal">
            para Niños
          </span>
        </h1>

        <!-- Subtitle -->
        <p class="text-lg md:text-xl text-teal-700 max-w-2xl mx-auto lg:mx-0 mb-8 leading-relaxed animate-fade-in-up" style="animation-delay: 0.6s;">
          Descubre las hermosas historias de la Biblia contadas en el idioma de nuestros ancestros. 
          Aprende, diviértete y conecta con nuestras raíces culturales.
        </p>

        <!-- CTA Buttons -->
        <div class="flex flex-col sm:flex-row gap-4 justify-center lg:justify-start items-center mb-8 animate-fade-in-up" style="animation-delay: 0.8s;">
          <button class="group bg-gradient-to-r from-orange-400 to-orange-500 hover:from-orange-500 hover:to-orange-600 text-white px-8 py-4 rounded-full font-semibold text-lg shadow-lg hover:shadow-xl transform hover:scale-105 transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-orange-400 focus:ring-offset-2">
            <span class="flex items-center space-x-2">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.246 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"></path>
              </svg>
              <span>Comenzar a Leer</span>
            </span>
          </button>
          
          <button class="group bg-white/80 backdrop-blur-sm hover:bg-white text-teal-700 border-2 border-teal-300 hover:border-teal-400 px-8 py-4 rounded-full font-semibold text-lg shadow-lg hover:shadow-xl transform hover:scale-105 transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-teal-400 focus:ring-offset-2">
            <span class="flex items-center space-x-2">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.828 14.828a4 4 0 01-5.656 0M9 10h1m4 0h1m-6 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
              <span>Ver Historias</span>
            </span>
          </button>
        </div>
      </div>

      <!-- Right side - Books image -->
      <div class="relative flex justify-center lg:justify-end animate-fade-in-up" style="animation-delay: 0.4s;">
        <div class="relative">
          <!-- Main books image -->
          <div class="relative z-10">
            <div class="character-container">
              <img 
                src="/jesus.svg" 
                alt="Jesús - Historias bíblicas en quechua" 
                class="w-full max-w-sm lg:max-w-md xl:max-w-lg shadow-2xl transform rotate-3 hover:rotate-0 transition-all duration-500 hover:scale-105"
                style="display: block; height: auto;"
              />
            </div>
          </div>
          
          <!-- Fallback decorative books if image doesn't exist -->
          <div class="books-fallback w-full max-w-md lg:max-w-lg xl:max-w-xl h-80 bg-gradient-to-br from-orange-200 to-yellow-200 rounded-2xl shadow-2xl transform rotate-3 hover:rotate-0 transition-all duration-500 hover:scale-105 flex items-center justify-center relative overflow-hidden">
            <!-- Decorative book stack -->
            <div class="absolute inset-0 flex items-center justify-center">
              <div class="flex space-x-2 transform -rotate-12">
                <!-- Book 1 -->
                <div class="w-16 h-20 bg-gradient-to-b from-orange-400 to-orange-500 rounded-sm shadow-lg transform rotate-12 hover:rotate-0 transition-transform duration-300"></div>
                <!-- Book 2 -->
                <div class="w-16 h-20 bg-gradient-to-b from-teal-400 to-teal-500 rounded-sm shadow-lg transform -rotate-6 hover:rotate-0 transition-transform duration-300" style="animation-delay: 0.1s;"></div>
                <!-- Book 3 -->
                <div class="w-16 h-20 bg-gradient-to-b from-yellow-400 to-yellow-500 rounded-sm shadow-lg transform rotate-6 hover:rotate-0 transition-transform duration-300" style="animation-delay: 0.2s;"></div>
                <!-- Book 4 -->
                <div class="w-16 h-20 bg-gradient-to-b from-red-400 to-red-500 rounded-sm shadow-lg transform rotate-12 hover:rotate-0 transition-transform duration-300" style="animation-delay: 0.3s;"></div>
              </div>
            </div>
            
            <!-- Floating elements around books -->
            <div class="absolute top-4 left-4 w-3 h-3 bg-yellow-300 rounded-full animate-pulse"></div>
            <div class="absolute bottom-4 right-4 w-2 h-2 bg-orange-300 rounded-full animate-pulse" style="animation-delay: 0.5s;"></div>
            <div class="absolute top-1/2 left-2 w-1.5 h-1.5 bg-teal-300 rounded-full animate-pulse" style="animation-delay: 1s;"></div>
          </div>
          
          <!-- Decorative elements around the image -->
          <div class="absolute -top-4 -right-4 w-8 h-8 bg-yellow-300 rounded-full animate-bounce" style="animation-delay: 0.5s;"></div>
          <div class="absolute -bottom-4 -left-4 w-6 h-6 bg-orange-300 rounded-full animate-bounce" style="animation-delay: 1s;"></div>
        </div>
      </div>
    </div>

   

    <!-- Scroll indicator -->
    <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce">
      <div class="w-6 h-10 border-2 border-teal-400 rounded-full flex justify-center">
        <div class="w-1 h-3 bg-teal-400 rounded-full mt-2 animate-pulse"></div>
      </div>
    </div>
  </div>
</section>

<style>
  @keyframes fade-in-up {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes twinkle {
    0%, 100% {
      opacity: 0.3;
      transform: scale(1);
    }
    50% {
      opacity: 1;
      transform: scale(1.2);
    }
  }

  .animate-fade-in-up {
    animation: fade-in-up 0.8s ease-out forwards;
    opacity: 0;
  }

  .animate-twinkle {
    animation: twinkle 2s ease-in-out infinite;
  }

  /* Books fallback styling */
  .books-fallback {
    display: none;
  }

  /* Show fallback when image doesn't exist */
  img[src="/books-image.jpg"]:not([src*="data:"]):not([src*="blob:"]):not([src*="http"]):not([src*="https"]) {
    display: none;
  }

  img[src="/books-image.jpg"]:not([src*="data:"]):not([src*="blob:"]):not([src*="http"]):not([src*="https"]) + .books-fallback,
  img[src="/books-image.jpg"][style*="display: none"] + .books-fallback {
    display: flex !important;
  }

  /* Gradient text effect */
  .gradient-text {
    background: linear-gradient(135deg, #0f766e, #f97316);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  /* Book stack animations */
  .books-fallback .flex > div {
    transition: all 0.3s ease;
  }

  .books-fallback:hover .flex > div {
    transform: rotate(0deg) !important;
  }

  /* Organic border effect for character image */
  img[src="/jesus.svg"] {
    filter: drop-shadow(0 10px 20px rgba(0, 0, 0, 0.15));
    transition: all 0.5s ease;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(249, 115, 22, 0.1) 0%, transparent 70%);
    display: block !important;
  }

  img[src="/jesus.svg"]:hover {
    filter: drop-shadow(0 15px 30px rgba(0, 0, 0, 0.2));
    transform: rotate(0deg) scale(1.05);
  }

  /* Alternative organic border with custom shape */
  .character-container {
    position: relative;
    display: inline-block;
    padding: 8px;
  }

  .character-container::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(45deg, #f97316, #ea580c, #fbbf24, #f59e0b);
    border-radius: 50%;
    z-index: -1;
    opacity: 0.3;
    animation: borderGlow 3s ease-in-out infinite;
  }

  @keyframes borderGlow {
    0%, 100% {
      opacity: 0.3;
      transform: scale(1);
    }
    50% {
      opacity: 0.5;
      transform: scale(1.05);
    }
  }
</style>

<script>
  // Handle books image fallback
  document.addEventListener('DOMContentLoaded', function() {
    const booksImage = document.querySelector('img[src="/jesus.svg"]') as HTMLImageElement;
    const booksFallback = document.querySelector('.books-fallback') as HTMLElement;
    
    if (booksImage) {
      console.log('Image found:', booksImage.src);
      
      // Check if image loads successfully
      booksImage.addEventListener('load', function() {
        console.log('Image loaded successfully');
        if (booksFallback) {
          booksFallback.style.display = 'none';
        }
      });
      
      booksImage.addEventListener('error', function() {
        console.log('Image failed to load');
        booksImage.style.display = 'none';
        if (booksFallback) {
          booksFallback.style.display = 'flex';
        }
      });
      
      // If image doesn't exist, show fallback immediately
      if (!booksImage.complete || booksImage.naturalWidth === 0) {
        console.log('Image not complete, showing fallback');
        booksImage.style.display = 'none';
        if (booksFallback) {
          booksFallback.style.display = 'flex';
        }
      }
    } else {
      console.log('Image element not found');
    }
  });
</script>
